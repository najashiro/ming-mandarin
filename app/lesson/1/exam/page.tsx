import { SiteShell, LessonHeader } from '@/components/SiteShell';
import { ExamClient } from '@/components/ExamClient';
import { getCurrentUser } from '@/app/auth';
import { getExamHistory } from '@/lib/server/persistence';
export default async function ExamPage(){const user=await getCurrentUser();const history=user?await getExamHistory(user):[];return <SiteShell><main><LessonHeader eyebrow="测验 · EXAMEN" title="Examen final · 100 puntos" description="Intentos ilimitados, historial real y mejor puntuación disponible para el ranking voluntario."/><section className="shell narrow"><ExamClient/></section>{user&&<section className="exam-history shell"><p className="eyebrow">HISTORIAL REAL</p><h2>Intentos anteriores</h2>{history.length?<div>{history.map(row=><article key={String(row.id)}><strong>{String(row.score)}/100</strong><span>{String(row.duration_seconds)} s</span><time>{new Date(String(row.created_at)).toLocaleDateString('es-PE')}</time></article>)}</div>:<p className="empty-state">Tu primer resultado aparecerá aquí.</p>}</section>}</main></SiteShell>}
