import { Hanzi } from '@/components/Hanzi';
import Link from 'next/link';
import { SiteShell, LessonHeader } from '@/components/SiteShell';
import { learningModules, howYouLearn } from '@/seed/modules';
import { CommunityButton, CommunityContextProvider } from '@/components/community/CommunityProvider';

export default function LessonRoute(){return <SiteShell><CommunityContextProvider context={{lessonId:1,section:'route',route:'/lesson/1'}}><main><LessonHeader title="你最近怎么样？" pinyin="Nǐ zuìjìn zěnmeyàng?" description="Aprende a presentarte, preguntar cómo está alguien y responder con naturalidad."/><div className="community-page-action shell"><CommunityButton label="Preguntar sobre la ruta"/></div><section className="module-grid lesson-modules shell">{learningModules.map(module=><Link className="module-card is-open" href={module.href} key={module.number}><span className="module-number">{String(module.number).padStart(2,'0')}</span><div><h2><Hanzi>{module.title}</Hanzi></h2><p><Hanzi>{module.subtitle}</Hanzi></p></div><div className="concept-row">{module.concepts.map(concept=><span key={concept}><Hanzi>{concept}</Hanzi></span>)}</div></Link>)}</section><section className="how-section shell"><p className="eyebrow">CÓMO APRENDERÁS</p><h2>Nueve acciones, una sola ruta</h2><div>{howYouLearn.map(([number,title,description])=><article key={number}><span>{number}</span><h3><Hanzi>{title}</Hanzi></h3><p><Hanzi>{description}</Hanzi></p></article>)}</div></section></main></CommunityContextProvider></SiteShell>}
