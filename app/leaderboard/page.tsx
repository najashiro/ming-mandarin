import { SiteShell, LessonHeader } from '@/components/SiteShell';
import { getLeaderboard } from '@/lib/server/persistence';
export default async function LeaderboardPage(){const rows=await getLeaderboard();return <SiteShell><main><LessonHeader eyebrow="排行榜 · RANKING" title="Dominio real" description="Participación voluntaria. Orden por mejor nota y fecha del mejor resultado; los empates comparten posición."/><section className="ranking shell"><div className="ranking-head"><span>Pos.</span><span>Estudiante</span><span>Mejor</span><span>Perfectos</span></div>{rows.length?rows.map((row)=><article key={`${row.display_name}-${row.rank}`}><b>#{String(row.rank)}</b><span>{String(row.display_name)}</span><strong>{String(row.best_score)}/100</strong><span>{String(row.perfect_exam_count)}</span></article>):<div className="empty-state"><h2>Aún no hay participantes</h2><p>El ranking aparecerá cuando un estudiante opte por participar y complete un examen.</p></div>}</section></main></SiteShell>}
