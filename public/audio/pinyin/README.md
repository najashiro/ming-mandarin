# Audio del repaso fonético

Los MP3 de este directorio se generan con `npm run audio:generate` a partir de
`data/pronunciation.json`. La clave de OpenAI se usa solo durante la generación
y nunca forma parte de la aplicación publicada.
