'use client';
import { useEffect } from 'react';
async function flushOfflineAttempts(){const key='ming-offline-attempts';let queue:unknown[];try{const stored=JSON.parse(localStorage.getItem(key)??'[]');if(!Array.isArray(stored))return;queue=stored;}catch{return;}if(!queue.length)return;const pending=[];for(const attempt of queue){try{const response=await fetch('/api/practice',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(attempt)});if(!response.ok)pending.push(attempt);}catch{pending.push(attempt);}}try{localStorage.setItem(key,JSON.stringify(pending));}catch{/* Unavailable storage must not block study. */}}
export function PwaRegister(){useEffect(()=>{if('serviceWorker' in navigator)navigator.serviceWorker.register('/sw.js').catch(()=>undefined);flushOfflineAttempts();window.addEventListener('online',flushOfflineAttempts);return()=>window.removeEventListener('online',flushOfflineAttempts);},[]);return null;}
